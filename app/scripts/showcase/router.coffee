Showcase.Router.map(() ->
    @resource 'show_components', ->
        @route 'alert'
        @route 'label'
        @route 'badge'
        @route 'well'
        @route 'panel'
        @route 'page-header'
        @route 'button'
        @route 'buttonGroup'
        @route 'progressbar'
        @route 'pills'
        @route 'tabs'
        @route 'notifications'
)