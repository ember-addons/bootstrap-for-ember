{{#each item in controller}}
    <li>
        {{#link-to item.route}}
            {{item.title}}
            {{#if item.special}}
                {{bs-label content="Special" type="danger"}}
            {{/if}}
        {{/link-to}}
    </li>
{{/each}}